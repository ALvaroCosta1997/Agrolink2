# Dependencies
node_modules/

# Build output
dist/

# Environment / secrets — never commit these
.env
.env.*
supabase/functions/.env

# OS
.DS_Store
Thumbs.db

.vercel
.env*.local

# Local Claude Code settings (machine-specific, not for the repo)
.claude/settings.local.json

# Supabase temp files (local CLI artefacts)
supabase/.temp/
